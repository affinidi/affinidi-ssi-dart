name: release
on:
  push:
    branches:
      - test/dart-security-scanner
jobs:
  dart-pipeline:
    uses: affinidi/pipeline-dart/.github/workflows/release.yaml@test/dart-scanner-non-blocking
    secrets: inherit
