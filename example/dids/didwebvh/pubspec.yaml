name: didwebvh_example
description: A sample command-line application with basic argument parsing.

publish_to: "none"

environment:
  sdk: ^3.8.0

dependencies:
  args: ^2.7.0
  ssi:
    path: ../../../
dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
